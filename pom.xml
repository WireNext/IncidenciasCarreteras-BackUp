<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Información general del proyecto -->
    <groupId>com.tuempresa</groupId> <!-- Cambia a tu propio grupo -->
    <artifactId>dgt-data-fetcher</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>DGT Data Fetcher</name>
    <description>Proyecto para obtener datos de la DGT y generar un archivo GeoJSON</description>

    <!-- Dependencias del proyecto -->
    <dependencies>
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient</artifactId>
            <version>4.5.13</version> <!-- Asegúrate de tener la versión correcta -->
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpcore</artifactId>
            <version>4.4.13</version>
        </dependency>
        <dependency>
            <groupId>javax.xml.bind</groupId>
            <artifactId>jaxb-api</artifactId>
            <version>2.3.1</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.11.3</version>
        </dependency>
    </dependencies>

    <!-- Configuración del plugin para empaquetar el JAR -->
    <build>
        <plugins>
            <!-- Plugin para empaquetar el JAR ejecutable con el atributo Main-Class -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Main-Class>com.tuempresa.DGTDataFetcher</Main-Class> <!-- Cambia a tu clase principal -->
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <!-- Plugin para compilar y empaquetar el proyecto -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>17</source> <!-- Cambia a la versión de Java que estés usando -->
                    <target>17</target> <!-- Cambia a la versión de Java que estés usando -->
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
